// @ts-check
import { defineConfig } from 'astro/config';

// Preview lives at https://i-am-crumbles.github.io/portfolio-v2/.
// At cutover (moving this into I-Am-Crumbles.github.io) remove `base`.
export default defineConfig({
  site: 'https://i-am-crumbles.github.io',
  base: '/portfolio-v2',
});
